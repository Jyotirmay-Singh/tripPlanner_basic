# Installs the production dependency closure for local tests. httpx now lives in requirements.txt
# because the push dispatcher uses its asynchronous client at runtime.
#
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pypdf==6.14.2
